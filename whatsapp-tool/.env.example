# Port the HTTP server listens on (default: 3000)
PORT=3000

# Optional: protect the API with a secret key.
# If set, all /api/* requests must include header: x-api-key: <your-key>
# Leave empty for unrestricted local access.
API_KEY=
